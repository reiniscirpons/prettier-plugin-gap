rec(torsionCoefficients := []);
rec(label := [[1, 2, 3], [0, 1]], index := 25);
rec(
  attr := [],
  content := [
      "A", "Address", "Alt", "Arg", "B", "Br", "Button", "C", "Cite", "Code",
      "Display", "E", "Email", "Emph", "Enum", "Example", "F", "File",
      "Homepage", "Ignore", "Index", "K", "Keyword", "Label", "List", "Listing",
      "Log", "M", "Math", "P", "PCDATA", "Package", "Par", "Q", "Quoted", "Ref",
      "Table", "URL", "Verb"
    ],
  name := "Subtitle",
  reqattr := [],
  type := "mixed"
);
rec(pcp := pcp, conjugator := conjugator, gens_mod := gens_mod);
rec();
rec(label := [1, 1], index := 14);
rec();
rec();
rec(success := true, result := exec.output);
rec(schreier := true, storenumbers := true);
rec();
rec(type := [3, 2], f := liealg_hom(L, K, [x, y, x * y], Basis(K)));
rec(acting := true);
rec(acting := true);
rec(type := [6, 31, b / c], f := liealg_hom(K, N, Basis(K), imgs));
rec();
rec(cf := U, ti := [IdentityMat(d, F)]);
rec(label := "G2[theta^2]", index := 7);
rec(r1 := [], rv1 := []);
rec(highlights := arr);
rec(
  DigraphNrEdges := 62,
  DigraphDiameter := 11,
  DigraphNrVertices := 32,
  IsPlanarDigraph := true,
  IsRegularDigraph := false,
);
rec(
  name := "M22",
  fields := [5],
  dimensions := [133],
  usemax := [2, 8, 5],
  issimple := true,
  size := 443520,
  atlasrepnrs := [29],
  values := [[5, 77], [7, 672], [9, 231]]
);
rec(
  d := 1,
  expl := [
      InfoLine(
        n,
        k,
        1,
        Concatenation("entire space GF(", String(q), ")^", String(n)),
        spaces,
        prefix
      )
    ],
  cons := [WholeSpaceCode, [n, q]]
);
rec(firstallowed := firstallowed, step := step, number := min_number);
rec(
  DigraphNrEdges := 756,
  DigraphNrVertices := 189,
);
rec(
  name := "U3(4):2",
  parent := "G24",
  generators := [
      ((a * b * a * b * a * b * a * b ^ 2 * a * b * a * b ^ 2 * a * b ^ 2) ^ 3) ^
      ((a * b) ^ 4),
      ((a * b * a * b * a * b ^ 2 * a * b * a * b ^ 2) ^ 10) ^ ((a * b ^ 2) ^ 4)
    ],
  order := 124800,
  index := 2016
);
rec(
  rts0 := pi[k],
  rts1 := [],
  start := 0,
  sp := VectorSpace(Rationals, pi[k], zero)
);
rec(D := DClass(R, MultiplicativeZero(R)), number := true);
rec(
  BookName := "LieAlgDB",
  ArchiveURLSubset := ["doc"],
  HTMLStart := "doc/chap0.html",
  PDFFile := "doc/manual.pdf",
  SixFile := "doc/manual.six",
  LongTitle := "A library of Lie algebras",
  Autoload := true
);
rec(
  text\/html := texthtml,
  text\/plain := textplain,
  metadata := rec(text\/html := "", text\/plain := "")
);
rec(chevSys := [pv, nv, hs], rank := Length(SimpleSystem(rs)));
rec(
  source := "[CR84, 15.29]",
  rels := [
      a ^ 2, b ^ 3, (a * b) ^ 19, ((a * b) ^ 2 * a * b ^ -1) ^ 6,
      ((a * b) ^ 3 * a * b ^ -1) ^ 6,
      (Comm(a, (b * a) ^ 4 * b ^ -1 * a * b)) ^ 2,
      (
        (a * b) ^ 4 * a * b ^ -1 * a * b * (a * b ^ -1) ^ 3 * (a * b) ^ 2 * a *
        b ^ -1
      ) ^ 2
    ],
  sgens := [a, b ^ ((a * b ^ -1 * a * b) ^ 2 * a * b ^ -1 * (a * b) ^ 2)]
);
rec(
  align := "tc",
  rows := [BrowseData.ReplacedEntry(subentry[1][3], ["*", ""], ["-", "+"])]
);
rec(
  entities := ["homalg", "CAP"],
  MainPage := false,
);
rec(
  DigraphNrVertices := 10,
  DigraphSource := [2, 2, 3, 3, 3, 5, 7, 7, 7, 7, 9, 9, 9, 9, 9],
  DigraphRange := [2, 2, 6, 8, 2, 4, 2, 6, 8, 6, 8, 5, 8, 2, 4]
);
rec(
  torso := ShallowCopy(pphi),
  upper := upper,
  normalsubgroup := [1 .. 4],
  nonfaithful := cchi
);
rec(
  dimension := C!.dimension,
  filteredDimension := R!.filteredDimension,
  boundary := C!.boundary,
  pseudoBoundary := R!.pseudoBoundary,
  properties := [
      ["length", EvaluateProperty(R, "length")],
      ["connected", true],
      ["type", "chainComplex"],
      ["filtration_length", ln],
      ["initial_inclusion", EvaluateProperty(R, "initial_inclusion")],
      ["characteristic", EvaluateProperty(R, "characteristic")]
    ]
);
rec(
  LastName := "Gamble",
  FirstNames := "Greg",
  IsAuthor := true,
  IsMaintainer := false,
  Email := "Greg.Gamble@uwa.edu.au",
  PostalAddress := Concatenation(
      "Department of Mathematics and Statistics\n",
      "Curtin University\n",
      "GPO Box U 1987\n",
      "Perth WA 6845\n",
      "Australia"
    ),
  Place := "Perth",
  Institution := "Curtin University"
);
rec(
  identifier := libtbl.identifier,
  text := libtbl.text,
  prime := libtbl.prime,
  size := reg.size,
  centralizers := reg.centralizers,
  orders := reg.orders,
  classes := reg.classes,
  powermap := reg.powermap,
  fusions := [
      rec(
        name := ordtbl.identifier,
        map := GetFusionMap(reg, ordtbl),
        type := "choice"
      )
    ],
  irreducibles := [],
  irredinfo := [],
  blocks := [],
  ordinary := ordtbl,
  operations := BrauerTableOps
);
