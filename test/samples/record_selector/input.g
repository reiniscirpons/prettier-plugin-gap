r.map;

fsa.states;

NCurses.keys.RIGHT;

S.genlabels;

emb.solutions;

GAPInfo.PackagesLoaded.browse;

NCurses.keys;

rep.setup.pairreps;

SCBistellarOptions.MaxInterval;

TextAttr.reset;

nk.END;

SingularDataTypes.(type_output);

IRREDSOL_DATA.GROUPS_DIM1;

SCIntFunc.MapBoundaries;

BrowseData.actions;

norb[i].rep;

r.map;

SOTRec.allGroupsP2Q2;

arec.nrows;

lines.start;

Pcgs_OrbitStabilizer(arg[1],NaturalActedSpace(acts,[arg[2]]),
                                arg[2],acts,act).stabpcgs;

EquivalenceClassRelation(C)!.
                data.comps;

EvaluateBySCSCP( "MathieuGroup", [ 24 ], 
                 laptop, port : output := "cookie" ).object;

speed.
      if;

RUN_FROIDURE_PIN(GapFroidurePin(S), -1,
                          InfoLevel(InfoSemigroups) > 0).rules;

EvaluateBySCSCP( "SylowSubgroup", [ M24, 2 ], 
                 laptop, port : output := "cookie" ).object;

Pcgs_MutableOrbitStabilizerOp(G,false,pt,
                                       Pcgs(G),Pcgs(G),op).stabilizer;

AtlasOfGroupRepresentationsInfo.characterinfo.(
                      record.groupname );

First( ProjectivesInfo( eintr ),
                       x -> x.name = tables[i][3] ).chars;

POL_CompleteRuntime2(
                    BCH_ComputeStructureConstants,
                    [recBCH, recLieAlg]  ).time;

RUN_FROIDURE_PIN(GapFroidurePin(S), -1,
                          InfoLevel(InfoSemigroups) > 0).elts;

IrreducibleCharactersOfIsoclinicGroup( Irr( modtbl ),
                          centre, outer, xpos, source.p, source.k ).all;

EvaluateBySCSCP( "MathieuGroup", [ 24 ], 
                 server, port : output := "cookie" ).object;

First( t.dynamic.categories[2],
                x -> [x.pos, x.level] = t.dynamic.selectedCategory ).value;

EvaluateBySCSCP( "IO_UnpickleStringAndPickleItBack", 
    [ IO_PickleToString(x) ], "localhost", port ).object;

First( AtlasOfGroupRepresentationsInfo.notified,
                 r -> r.ID = "core" ).DataURL;

faster.
      if;

EvaluateBySCSCP( "IO_UnpickleStringAndPickleItBack", 
    [ IO_PickleToString(x) ], server, port ).object;

AtlasDataGAPFormatFile(
                                       strings[1], "string" ).generators;

EvaluateBySCSCP( "IO_UnpickleStringAndPickleItBack", 
    [ IO_PickleToString(x) ], "localhost", port ).object;

hello.world.
foo.bar.1234567890.
  1234567890
  .1234567890
  .1234567890
  .1234567890.1234567890.1234567890;
