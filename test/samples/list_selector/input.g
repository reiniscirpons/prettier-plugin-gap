mxstd[1];

cen[j][i];

y[i];

Y!.boundaries[k+1];

To[p+Position(P, P[i])];

name[2];

Elements(BI)[j];

word[len+i];

HWO7Mat[2];

SCTL[u1][u3][2][t3];

T[i-1];

To[Position(P, P[i])][p+Position(P,K[j])][2][1];

mealym[1][1];

mu[j][k];

MonoidPolys( mp )[i];

B[i+1][j-1];

P[i];

polygons[i];

paras[i][2];

To[Position(P, K[i])][Position(P,K[j])];

LST[HAP_Binlisttoint([LTP,TP,RTP,LP,P,RP,LBP,BP,RBP,LT,T,
RT,L,R,LB,B,RB,LTF,TF,RTF,LF,F,RF,LBF,BF,RBF])+1];

String(SourceOfPath(ArrowsOfQuiver(Q)[
        SIntChar(temp[Length(temp)]) - SIntChar('a') + 1]))[2];

ArrowsOfQuiver(Q)[SIntChar(input_str[i + 1])
                      - SIntChar('A') + 1];

String(SourceOfPath(ArrowsOfQuiver(Q)[
        SIntChar(input_str[1]) - SIntChar('A') + 1]))[2];

genmag2[ RemInt( j-1, nag ) + 1  
            + nag * (( QuoInt( j-1, nag ) + 1 )^pi - 1 ) ];

String(SourceOfPath(ArrowsOfQuiver(Q)[
        SIntChar(temp[Length(temp)]) - SIntChar('a') + 1]))[2];

ArrowsOfQuiver(Q)[SIntChar(input_str[i])
                      - SIntChar('A') + 1];

LeadingUEALatticeMonomial(
                                  novar, x )[ 4 ];

Coefficients(Basis(SubspaceNC(L,[newcg[1][i]],"basis"),[newcg[1][i]]), 
                                    theta(newcg[1][i] ))[1];

Coefficients(Basis(SubspaceNC(L,[newcg[1][i[2]]],"basis"),[newcg[1][i[2]]]),
                                      theta(newcg[1][i[1]]))[1];

Coefficients( Basis( Subspace(L,[es[j]]), [es[j]] ), 
                       hs[i]*es[j] )[1];

Coefficients(Basis(SubspaceNC(L,[newcg[2][i[1]]],"basis"),[newcg[2][i[1]]]), 
                                       sigma(newcg[1][i[2]] ))[1];

mon[ Position( gflist, t[ i ] ) ]
                                              [ d - i + 1 ];

Coefficients(Basis(SubspaceNC(L,[newcg[1][i[2]]],"basis"),[newcg[1][i[2]]]),
                                      theta(newcg[1][i[1]]))[1];

HomomorphismDigraphsFinder(DigraphSymmetricClosure(CycleDigraph(5)),
                                   D,
                                   fail,
                                   [],         
                                   1,       
                                   fail,
                                   0,
                                   [1 .. 8],
                                   ListTransformation(t, 5),
                                   fail,
                                   fail)[1];

t.dynamic.indexRow[
                                      t.dynamic.selectedCategory[1] ];

numbers[ Position( labels,
                                               classnames[ img ] ) ];

invfus[ Position( parts,
                                  PowerPartition( parts[ fus[i] ], p ) ) ];

HomomorphismDigraphsFinder(digraph,
                                    digraph,
                                    hook,
                                    S,
                                    infinity,
                                    fail,
                                    false,
                                    DigraphVertices(digraph),
                                    [],
                                    fail,
                                    fail)[1];

HomomorphismDigraphsFinder(CompleteDigraph(25),
                                 D,
                                 fail,       
                                 [],         
                                 1,          
                                 fail,         
                                 0,          
                                 [1 .. 280], 
                                 OnTuples([1 .. i], t),         
                                 fail,       
                                 fail)[1];
